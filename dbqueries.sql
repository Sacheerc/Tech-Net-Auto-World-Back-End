-- Create table userInfo
CREATE TABLE `tech_net`.`userinfo` (`userId` INT NOT NULL , `userName` VARCHAR(300) NOT NULL , `password` VARCHAR(300) NOT NULL , `email` VARCHAR(300) NOT NULL , PRIMARY KEY (`userId`)) ENGINE = InnoDB;
